# 本地开发负责协议端口与可读日志；数据库由 postgresql Profile 决定。
logging:
  config: classpath:logback-spring.xml
  level:
    com.example.bms: INFO
    org.hibernate.SQL: WARN

bms:
  demo-data:
    initialize: true
  protocol:
    syslog:
      port: 5514
    snmp:
      trap-port: 1162


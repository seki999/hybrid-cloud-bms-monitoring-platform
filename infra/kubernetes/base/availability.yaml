# 通过 PodDisruptionBudget 与副本可用性约束，降低节点维护期间同时中断所有应用实例的风险。
# 该保护只覆盖自愿驱逐，仍需结合副本数、探针和跨节点调度实现完整高可用。
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: {name: bms-web-app, namespace: bms-monitoring}
spec:
  minAvailable: 1
  selector: {matchLabels: {app: bms-web-app}}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: {name: bms-web-app, namespace: bms-monitoring}
spec:
  scaleTargetRef: {apiVersion: apps/v1, kind: Deployment, name: bms-web-app}
  minReplicas: 2
  maxReplicas: 6
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: {type: Utilization, averageUtilization: 70}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bms-web-app
  namespace: bms-monitoring
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  rules:
    - host: bms.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend: {service: {name: bms-web-app, port: {name: http}}}

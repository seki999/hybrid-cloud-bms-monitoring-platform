# 为平台资源创建独立命名空间，隔离名称、配额、网络策略和运维操作范围。
# 所有基础清单应引用同一命名空间，确保删除或审计时边界清晰。
apiVersion: v1
kind: Namespace
metadata:
  name: bms-monitoring
  labels:
    app.kubernetes.io/part-of: hybrid-cloud-bms-monitoring-platform
    pod-security.kubernetes.io/enforce: restricted
